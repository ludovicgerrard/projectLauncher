package com.zktechnology.android.verify.utils;

public enum ZKVerConState {
    STATE_WAIT,
    STATE_WANT,
    STATE_USER,
    STATE_WAY,
    STATE_ACTION,
    STATE_RECORD,
    STATE_REMOTE_AUTH,
    STATE_DELAY
}
